/gradlew text eol=lf
*.bat text eol=crlf
*.jar binary

# 这些必须是 LF：都是在 Linux/WSL 里直接执行的。仓库有 core.autocrlf=true，
# 且脚本常从 Windows 侧编辑器经 \\wsl$ 共享改动，两者都会引入 CRLF，导致
# bash "syntax error near unexpected token $'{\r'"。显式钉死 eol=lf 与编辑
# 环境和本地 git 配置无关，克隆到任何机器都成立。
*.sh text eol=lf
*.py text eol=lf
*.properties text eol=lf
# 同理钉死源码/配置/文档，避免经 \\wsl$ 编辑引入的 CRLF 制造无意义的 diff。
# 不含 *.sql：已应用的迁移文件改行尾会变 Flyway 校验和，触发 checksum mismatch。
*.kt text eol=lf
*.kts text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.md text eol=lf
Makefile text eol=lf
Dockerfile text eol=lf
